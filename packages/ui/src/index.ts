/**
 * @studiq/ui — Shared UI primitives for StudiQ.
 *
 * This package will contain:
 * - shadcn/ui components (button, dialog, table, etc.)
 * - Layout components (DashboardLayout, Navbar, Sidebar, etc.)
 * - Shared hooks (use-mobile, use-toast)
 * - Utility functions (cn helper)
 * - Global CSS (globals.css)
 *
 * Actual code extraction happens in C2.
 */
export {};
