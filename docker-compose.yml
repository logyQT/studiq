services:
  jaeger:
    image: jaegertracing/all-in-one:1.60
    ports:
      - "4318:4318"
      - "16686:16686"
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    volumes:
      - jaeger_data:/tmp/jaeger

volumes:
  jaeger_data:
