import { EditQuizClient } from '@/app/(frontend)/edu/quizzes/[id]/edit/edit-client';

export const metadata = {
  title: 'Edit Quiz',
};

export default function EditQuizPage() {
  return <EditQuizClient />;
}
