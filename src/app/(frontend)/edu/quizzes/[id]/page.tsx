import { QuizDetailClient } from '@/app/(frontend)/edu/quizzes/[id]/detail-client';

export const metadata = {
  title: 'Quiz',
};

export default function QuizDetailPage() {
  return <QuizDetailClient />;
}
